'use strict'


/* eslint-env mocha */
const leaveTests = require('.')

describe('leave-tests', () => {
  it('write this test', () => {
    console.assert(leaveTests, 'should export something')
  })
})
